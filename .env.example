# VAPID Keys (generate with: npm run gen-vapid)
VAPID_PUBLIC_KEY=your_public_key_here
VAPID_PRIVATE_KEY=your_private_key_here

# Timezone (WIB=7, WITA=8, WIT=9)
DEFAULT_TIMEZONE=WITA

# Test mode (check every N seconds, 0=disabled)
TEST_INTERVAL_SECONDS=0

# Port
PORT=3000
